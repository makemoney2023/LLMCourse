export function HomeProblem() {
  return (
    <section className="border-b border-border/70 bg-background/70">
      <div className="mx-auto max-w-6xl px-4 py-20 sm:px-6 lg:grid lg:grid-cols-[0.9fr_1.1fr] lg:gap-16 lg:py-28">
        <h2 className="font-heading text-3xl tracking-tight sm:text-4xl">
          Tool access isn’t the same as capability.
        </h2>
        <p className="mt-6 max-w-2xl text-lg leading-8 text-muted-foreground lg:mt-0">
          Licenses without a shared standard → reinvented prompts, swinging
          quality, invented facts. Prompt-only training doesn’t stick when tools
          change.
        </p>
      </div>
    </section>
  );
}
